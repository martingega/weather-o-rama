package org.weather.displays;

/**
 * The DisplayElement interface just includes one method named display()
 * that we will call when the display element needs to be displayed
 */
public interface DisplayElement {

    void display();
}
